// Змінна, що може приймати рядок або число (union type)
let union: string | number;

// Змінна, що може приймати тільки одне з двох рядкових значень: 'enable' або 'disable' (literal type)
let literal: 'enable' | 'disable';
